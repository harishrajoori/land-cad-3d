# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp

# Node (in case tooling is added later)
node_modules/
npm-debug.log*

# Secrets — never commit API keys (e.g. OpenAI key for GPT Image)
.env
.env.*
*.key
secrets.json

# Kiro local editor/MCP settings (not part of the shared project)
.kiro/settings/
